<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/client/css/bootstrap.min.css" />
    <title>Home</title>
  </head>
  <body>
    <div
      style="
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin: 25px;
      "
    >
      <h1>Welcome to the Home Page</h1>
      <div>
        <a href="/create-user" class="btn btn-warning">Create User</a>
      </div>
    </div>
    <hr />
    <div>
      <table class="table table-striped table-hover table-bordered" style="margin: 25px">
        <thead>
          <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Username</th>
            <th>Address</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(user => { %>
          <tr>
            <td><%= user.id %></td>
            <td><%= user.fullName %></td>
            <td><%= user.username %></td>
            <td><%= user.address %></td>
            <td>
              <div class="d-flex gap-5">
                <form action="/handle-view-user/<%= user.id %>" method="GET">
                  <button type="submit" class="btn btn-success">View</button>
                </form>
                <form action="/handle-delete-user/<%= user.id %>" method="POST">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
              </div>
            </td>
          </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
    <script src="/client/javascript/bootstrap.bundle.min.js"></script>
  </body>
</html>
